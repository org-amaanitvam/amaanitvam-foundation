import mongoose from 'mongoose'; import User from './models/user.js'; import Department from './models/department.js'; mongoose.connect('mongodb+srv://techamaanitvam:TechAmaanitvam116@amaanitvam.z9utf07.mongodb.net/amaanitvam?retryWrites=true&w=majority&appName=Amaanitvam').then(async () => { const department = await Department.findOne({ departmentName: 'Management' }).populate('departmentHead', 'name email role department'); const departmentUsers = await User.find({ department: department.departmentName, status: 'active' }).select('name email role department'); const userMap = new Map(); if (department.departmentHead) { const head = { _id: department.departmentHead._id, name: department.departmentHead.name, email: department.departmentHead.email, role: department.departmentHead.role, departmentId: department._id, departmentName: department.departmentName, }; userMap.set(head._id.toString(), head); } departmentUsers.forEach((user) => { const id = user._id.toString(); if (!userMap.has(id)) { userMap.set(id, { _id: user._id, name: user.name, email: user.email, role: user.role, departmentId: department._id, departmentName: department.departmentName, }); } }); console.log('Management Users:', Array.from(userMap.values())); process.exit(0); });
